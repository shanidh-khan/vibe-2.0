// import { config } from "dotenv";
// config({ path: `.env.${process.env.NODE_ENV || "development"}.local` });
// const {
//     PORT = 3000,
//     NODE_ENV = "development",
//     MONGO_URI = "mongodb://localhost:27017/express-mongo",
//     JWT_SECRET = "secret",
//     JWT_EXPIRE = "30d",
//   } = process.env;

//   export { PORT, NODE_ENV, MONGO_URI, JWT_SECRET, JWT_EXPIRE };
